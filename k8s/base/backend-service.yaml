apiVersion: v1
kind: Service
metadata:
  name: backend-service
  namespace: local-buyer-intelligence
  labels:
    app: backend
spec:
  type: ClusterIP
  selector:
    app: backend
  ports:
    - port: 8000
      targetPort: 8000
      protocol: TCP
      name: http
