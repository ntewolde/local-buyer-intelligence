apiVersion: networking.k8s.io/v1
kind: Ingress
metadata:
  name: lbi-ingress
  namespace: local-buyer-intelligence
  annotations:
    kubernetes.io/ingress.class: nginx
    nginx.ingress.kubernetes.io/proxy-body-size: "50m"
    nginx.ingress.kubernetes.io/proxy-read-timeout: "300"
    # TLS disabled for nip.io development - enable for production with real domain
    # cert-manager.io/cluster-issuer: letsencrypt-prod
spec:
  # TLS disabled for nip.io - uncomment for production with real domain
  # tls:
  #   - hosts:
  #       - api.YOUR-IP.nip.io
  #       - app.YOUR-IP.nip.io
  #     secretName: lbi-tls-secret
  rules:
    # For minikube: run "minikube tunnel" then access via 127.0.0.1.nip.io
    - host: api.127.0.0.1.nip.io
      http:
        paths:
          - path: /
            pathType: Prefix
            backend:
              service:
                name: backend-service
                port:
                  number: 8000
    - host: app.127.0.0.1.nip.io
      http:
        paths:
          - path: /
            pathType: Prefix
            backend:
              service:
                name: frontend-service
                port:
                  number: 3000
